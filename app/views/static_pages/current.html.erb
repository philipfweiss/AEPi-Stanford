

<!-- Trigger the modal with a button -->

<!-- Modal -->
<div id="myModal" class="modal fade" role="dialog">
    <div class="modal-dialog">

        <!-- Modal content-->
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal">&times;</button>
                <h4 class="modal-title">Modal Header</h4>
            </div>
            <div class="modal-body">
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
            </div>
        </div>

    </div>
</div>


<div class="container" style="padding-left: 5%; padding-right: 5%; padding-bottom: 5%">
    <br>
    <div style="margin-bottom: -5px;" class="alert alert-success">To Contact Us: <a href="/contact-us"> Click Here. </a> <br> Click on boxes below to read bios!</div>
    <hr>



    <% counter = 0%>
    <% @brothers.each do |k,v|%>

    <% if v[:leadership]%>
            <div id="<%=k.gsub(/\s+/, "")%>" class="col-md-6">
                <div class="wrapper spec click ">
                    <div class="image-wrapper click-hide">
                        <img src=<%= v[:image] ? image_path(v[:image]) : "//cdn0.iconfinder.com/data/icons/user-pictures/100/male-256.png" %>  />
                        <div>
                            <div>
                                <h1 class="name"><%= k %> <%= v[:year]%></h1>
                                <p class="description"><%= v[:title] ? v[:title] : "　" %></p>
                            </div>
                        </div>
                    </div>
                    <div class="click-display">
                        hi
                    </div>
                </div>
            </div>
            <% next %>
    <% end %>

    <% if (counter+=1) % 3 == 1%>
        <div id="<%=k.gsub(/\s+/, "")%>" class="col-md-4">
            <div class="wrapper spec">
                <div class="image-wrapper">
                    <img src=<%= v[:image] ? image_path(v[:image]) : "//cdn0.iconfinder.com/data/icons/user-pictures/100/male-256.png" %>  />
                    <div>
                        <div>
                            <h1 class="name"><%= k %> <%= v[:year]%></h1>
                            <p class="description"><%= v[:title] ? v[:title] : "　" %></p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <% elsif (counter % 3 == 2) %>
        <div id="<%=k.gsub(/\s+/, "")%>" class="col-md-4">
            <div class="wrapper spec">
                <div class="image-wrapper">
                    <img src=<%= v[:image] ? image_path(v[:image]) : "//cdn0.iconfinder.com/data/icons/user-pictures/100/male-256.png" %>  />
                    <div>
                        <div>
                            <h1 class="name"><%= k %> <%= v[:year]%></h1>
                            <p class="description"><%= v[:title] ? v[:title] : "　" %></p>
                        </div>
                    </div>
                </div>
            </div>
        <% elsif (counter % 3 ==  0) %>

        </div>
            <div id="<%=k.gsub(/\s+/, "")%>" class="col-md-4">
                <div class="wrapper spec">
                    <div class="image-wrapper">
                        <img src=<%= v[:image] ? image_path(v[:image]) : "//cdn0.iconfinder.com/data/icons/user-pictures/100/male-256.png" %>  />
                        <div>
                            <div>
                                <h1 class="name"><%= k %> <%= v[:year]%></h1>
                                <p class="description"><%= v[:title] ? v[:title] : "　" %></p>
                            </div>
                        </div>

                    </div>
                </div>
            </div>
        <%end %>
    <% end %>
</div>

<script>
    var ready = function() {

        <% @brothers.each do |k,v| %>
            $('#<%=k.gsub(/\s+/, "")%>').on('click', function() {
                console.log('hello!');
                console.log('<%= v[:bio]%>');
                $('.modal-header').html('<h2><%= k %>\'s Bio: </h4>');
                $('.modal-body').html('<p class="bio-text"><%=raw(v[:bio])%></p>');
                $('#myModal').modal('toggle');
            });
        <% end %>

    };

    $(document).ready(ready);
    $(document).on('page:load', ready);

</script>